<Window x:Class="Dorothy.Views.ReachabilityWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Simple Reachability Test"
        Height="700" Width="900"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#1A2332">
    <Window.Resources>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#3A4555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#232F3E"/>
        </Style>

        <Style x:Key="CardHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#E8ECF1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="DefaultButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Background" Value="#3A4555"/>
            <Setter Property="Foreground" Value="#E8ECF1"/>
            <Setter Property="BorderBrush" Value="#4A5565"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A5565"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2A3444"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2A3444"/>
                    <Setter Property="Foreground" Value="#6A7A8A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DefaultButton}">
            <Setter Property="Background" Value="#5B8AF5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#5B8AF5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A79E4"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A68D3"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#3A4555"/>
                    <Setter Property="BorderBrush" Value="#3A4555"/>
                    <Setter Property="Foreground" Value="#6A7A8A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DefaultTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#3A4555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#2A3444"/>
            <Setter Property="Foreground" Value="#E8ECF1"/>
            <Setter Property="CaretBrush" Value="#E8ECF1"/>
            <Setter Property="SelectionBrush" Value="#5B8AF5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#5B8AF5"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DefaultLabel" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#8899AA"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <Style x:Key="DefaultComboBox" TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#3A4555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#2A3444"/>
            <Setter Property="Foreground" Value="#E8ECF1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Height" Value="32"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#5B8AF5"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DefaultCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#C8D1DC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DefaultRadioButton" TargetType="RadioButton">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#C8D1DC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1E2A3A"/>
            <Setter Property="Foreground" Value="#E8ECF1"/>
            <Setter Property="BorderBrush" Value="#3A4555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#1E2A3A"/>
            <Setter Property="AlternatingRowBackground" Value="#232F3E"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#2A3444"/>
            <Setter Property="VerticalGridLinesBrush" Value="#2A3444"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#232F3E"/>
            <Setter Property="Foreground" Value="#8899AA"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#3A4555"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="#E8ECF1"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="Transparent" Padding="4">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#253040"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2A3A50"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#2A3444"/>
            <Setter Property="Foreground" Value="#5B8AF5"/>
            <Setter Property="BorderBrush" Value="#3A4555"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="StepTitleTextBlock"
                       Text="Step 1: Define Vantage Point &amp; Target Network"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#E8ECF1"
                       Margin="0,0,0,8"/>
            <TextBlock x:Name="StepDescriptionTextBlock"
                       Text="Configure your vantage point and the target network you want to analyze."
                       FontSize="14"
                       Foreground="#8899AA"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.IsSharedSizeScope="True">

                <StackPanel x:Name="Step1Panel">

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Test Mode" Style="{StaticResource CardHeaderStyle}"/>
                            <RadioButton x:Name="ModeARadioButton" Content="Test a specific host or known network" Style="{StaticResource DefaultRadioButton}" Margin="0,0,0,8" IsChecked="True" Checked="ModeRadioButton_Checked"/>
                            <RadioButton x:Name="ModeBRadioButton" Content="Analyze boundary device only (target network unknown)" Style="{StaticResource DefaultRadioButton}" Checked="ModeRadioButton_Checked"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Vantage Point" Style="{StaticResource CardHeaderStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="Vantage Point Name:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,8"/>
                                <TextBox x:Name="VantagePointNameTextBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Margin="0,0,0,8" Text="LAN-Host"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Source NIC:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <ComboBox x:Name="SourceNicComboBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource DefaultComboBox}" SelectionChanged="SourceNicComboBox_SelectionChanged"/>
                            </Grid>

                            <Border x:Name="BoundaryPreviewBorder"
                                    Background="#2A3444"
                                    BorderBrush="#3A4555"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="12"
                                    Margin="0,12,0,0"
                                    Visibility="Collapsed">
                                <StackPanel>
                                    <TextBlock Text="Boundary Device" FontSize="14" FontWeight="SemiBold" Foreground="#E8ECF1" Margin="0,0,0,8"/>
                                    <TextBlock x:Name="BoundaryInfoTextBlock" FontSize="12" Foreground="#8899AA" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border x:Name="TargetNetworkCard" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Target Host / IP" Style="{StaticResource CardHeaderStyle}"/>
                            <TextBlock Text="Enter a single IP address or hostname for the primary target. CIDR notation (e.g. 192.168.1.0/24) is also accepted."
                                       FontSize="12" Foreground="#8899AA" Margin="0,0,0,10" TextWrapping="Wrap"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="Target Label:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,8"/>
                                <TextBox x:Name="TargetNetworkNameTextBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Margin="0,0,0,8" Text="Target Host"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Target Host / IP:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBox x:Name="TargetCidrTextBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Text="192.168.1.1"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border x:Name="ExternalTestIpCard" Style="{StaticResource CardStyle}" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="External Test Target" Style="{StaticResource CardHeaderStyle}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Content="External Test IP:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBox x:Name="ExternalTestIpTextBox" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Text="8.8.8.8"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border x:Name="KnownInsideIpsCard" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Additional Test Targets (Optional)" Style="{StaticResource CardHeaderStyle}"/>
                            <TextBlock Text="Add other hosts in the same network to test alongside the primary target."
                                       FontSize="12" Foreground="#8899AA" Margin="0,0,0,10" TextWrapping="Wrap"/>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="IP:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,8"/>
                                <TextBox x:Name="InsideIpTextBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Margin="0,0,8,8"/>
                                <Button x:Name="AddInsideIpButton" Grid.Row="0" Grid.Column="2" Content="Add" Width="80" Height="32" Style="{StaticResource DefaultButton}" Margin="0,0,0,8" Click="AddInsideIpButton_Click"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Label:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBox x:Name="InsideIpLabelTextBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Margin="0,0,8,0"/>
                            </Grid>

                            <DataGrid x:Name="InsideIpsDataGrid"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      MaxHeight="150">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="IP" Binding="{Binding AssetIpString}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="*" IsReadOnly="True"/>
                                    <DataGridTemplateColumn Header="Action" Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Remove" Style="{StaticResource DefaultButton}" Click="RemoveInsideIpButton_Click" Tag="{Binding}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="Step2Panel" Visibility="Collapsed">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="ICMP Reachability" Style="{StaticResource CardHeaderStyle}"/>
                            <TextBlock Text="We send ICMP echo (ping) to the target network gateway candidates and any known inside IPs to see if they respond."
                                       FontSize="14" Foreground="#8899AA" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            <Button x:Name="RunIcmpChecksButton" Content="Run ICMP Checks" MinWidth="140" Height="32" Style="{StaticResource PrimaryButton}" Margin="0,0,0,12" HorizontalAlignment="Left" Click="RunIcmpChecksButton_Click"/>
                            <ProgressBar x:Name="IcmpProgressBar" Height="6" Margin="0,0,0,12" Visibility="Collapsed" Maximum="100" Value="0"/>
                            <DataGrid x:Name="IcmpResultsDataGrid"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      MaxHeight="500">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="IP" Binding="{Binding TargetIpString}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Reachable" Binding="{Binding Reachable}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Sent/Received" Binding="{Binding Sent}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Avg RTT (ms)" Binding="{Binding AvgRttMs}" Width="*" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock x:Name="IcmpStatusTextBlock" FontSize="12" Foreground="#8899AA" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="Step3Panel" Visibility="Collapsed">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="TCP Reachability" Style="{StaticResource CardHeaderStyle}"/>
                            <TextBlock Text="We check basic TCP connectivity to common service ports. This detects reachability even if ICMP is blocked."
                                       FontSize="14" Foreground="#8899AA" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Content="Probe Ports:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBox x:Name="TcpProbePortsTextBox" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Text="22,23,25,53,80,110,135,139,143,443,445,993,995,1433,1521,3306,3389,5432,5900,8080,8443"/>
                            </Grid>
                            <Button x:Name="RunTcpChecksButton" Content="Run TCP Checks" MinWidth="140" Height="32" Style="{StaticResource PrimaryButton}" Margin="0,0,0,12" HorizontalAlignment="Left" Click="RunTcpChecksButton_Click"/>
                            <ProgressBar x:Name="TcpProgressBar" Height="6" Margin="0,0,0,12" Visibility="Collapsed" Maximum="100" Value="0"/>
                            <DataGrid x:Name="TcpResultsDataGrid"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      MaxHeight="500">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="IP" Binding="{Binding TargetIpString}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="State" Binding="{Binding State}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="RTT (ms)" Binding="{Binding RttMs}" Width="*" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="Step4Panel" Visibility="Collapsed">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Path Analysis" Style="{StaticResource CardHeaderStyle}"/>
                            <TextBlock Text="We trace the path towards the target network to identify the boundary device and where traffic stops."
                                       FontSize="14" Foreground="#8899AA" Margin="0,0,0,12" TextWrapping="Wrap"/>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Content="Target IP:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBox x:Name="PathTargetIpTextBox" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" IsReadOnly="True"/>
                            </Grid>
                            <ProgressBar x:Name="PathProgressBar" Height="6" Margin="0,0,0,12" Visibility="Collapsed" Maximum="100" Value="0"/>
                            <DataGrid x:Name="PathHopsDataGrid"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      MaxHeight="500">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Hop" Binding="{Binding HopNumber}" Width="Auto" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="IP" Binding="{Binding HopIpString}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="RTT (ms)" Binding="{Binding RttMs}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="*" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock x:Name="PathNotesTextBlock" FontSize="12" Foreground="#8899AA" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="Step5Panel" Visibility="Collapsed">

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Optional Deeper Scan" Style="{StaticResource CardHeaderStyle}"/>
                            <CheckBox x:Name="EnableDeeperScanCheckBox" Content="Enable deeper scan" Style="{StaticResource DefaultCheckBox}" Margin="0,0,0,12"/>
                            <Grid Margin="0,0,0,12" IsEnabled="{Binding IsChecked, ElementName=EnableDeeperScanCheckBox}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumn"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Label Content="Ports to scan:" Style="{StaticResource DefaultLabel}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBox x:Name="DeeperScanPortsTextBox" Grid.Column="1" Style="{StaticResource DefaultTextBox}" Height="32" Text="22,23,25,53,80,110,135,139,143,443,445,502,993,995,1433,1521,3306,3389,5432,5900,8080,8443"/>
                            </Grid>
                            <Button x:Name="RunDeeperScanButton" Content="Run deeper scan" MinWidth="140" Height="32" Style="{StaticResource PrimaryButton}" Margin="0,0,0,12"
                                    HorizontalAlignment="Left"
                                    IsEnabled="{Binding IsChecked, ElementName=EnableDeeperScanCheckBox}" Click="RunDeeperScanButton_Click"/>
                            <ProgressBar x:Name="DeeperScanProgressBar" Height="6" Margin="0,0,0,12" Visibility="Collapsed" Maximum="100" Value="0"/>
                            <DataGrid x:Name="DeeperScanResultsDataGrid"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      HeadersVisibility="Column"
                                      MaxHeight="350"
                                      IsEnabled="{Binding IsChecked, ElementName=EnableDeeperScanCheckBox}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="IP" Binding="{Binding TargetIpString}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Open Ports" Binding="{Binding OpenPortsString}" Width="*" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Closed" Binding="{Binding ClosedCount}" Width="Auto" IsReadOnly="True"/>
                                    <DataGridTextColumn Header="Filtered" Binding="{Binding FilteredCount}" Width="Auto" IsReadOnly="True"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Analysis Summary" Style="{StaticResource CardHeaderStyle}"/>
                            <TextBlock x:Name="SummaryTextBlock"
                                       FontSize="14"
                                       Foreground="#E8ECF1"
                                       TextWrapping="Wrap"
                                       Margin="0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="2"
                Background="#151D28"
                BorderBrush="#3A4555"
                BorderThickness="0,1,0,0"
                Padding="12,12,12,12"
                Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusLabel"
                           Text="Step 1 of 5"
                           FontSize="12"
                           Foreground="#8899AA"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BackButton" Content="Back" MinWidth="90" Height="32" Style="{StaticResource DefaultButton}" Margin="0,0,8,0" IsEnabled="False" Click="BackButton_Click"/>
                    <Button x:Name="NextButton" Content="Next" MinWidth="90" Height="32" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Click="NextButton_Click"/>
                    <Button x:Name="CancelButton" Content="Cancel" MinWidth="90" Height="32" Style="{StaticResource DefaultButton}" Click="CancelButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

