<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Dorothy.Views.SnmpWalkResultsWindow"
        Title="SNMP Walk Results" 
        Icon="/Resources/icon.ico"
        Height="600" Width="800"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Background="#F3F4F6">
    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
        </Style>

        <!-- Card Header Style -->
        <Style x:Key="CardHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Default Button Style -->
        <Style x:Key="DefaultButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DefaultButton}">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#111827"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="HeaderTextBlock" 
                       Text="SNMP Walk Results" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#111827" 
                       Margin="0,0,0,8"/>
            <TextBlock x:Name="SummaryTextBlock" 
                       FontSize="14" 
                       Foreground="#6B7280" 
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Results Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Success Card (if vulnerable) -->
                <Border x:Name="SuccessCard" 
                        Style="{StaticResource CardStyle}" 
                        Background="#FEF3C7" 
                        BorderBrush="#F59E0B"
                        IsVisible="False">
                    <StackPanel>
                        <TextBlock Text="⚠️ SNMP Vulnerability Found" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="#92400E" 
                                   Margin="0,0,0,8"/>
                        <TextBlock x:Name="VulnerabilityDetailsTextBlock" 
                                   FontSize="14" 
                                   Foreground="#92400E" 
                                   TextWrapping="Wrap" 
                                   Margin="0,0,0,12"/>
                        <TextBlock Text="OIDs Retrieved:" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="#92400E" 
                                   Margin="0,0,0,8"/>
                        <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="OidsTextBlock" 
                                       FontSize="12" 
                                       Foreground="#92400E" 
                                       TextWrapping="Wrap" 
                                       FontFamily="Consolas"/>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

                <!-- Secure Card (if not vulnerable) -->
                <Border x:Name="SecureCard" 
                        Style="{StaticResource CardStyle}" 
                        Background="#D1FAE5" 
                        BorderBrush="#059669"
                        IsVisible="False">
                    <StackPanel>
                        <TextBlock Text="✓ SNMP Secure" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Foreground="#065F46" 
                                   Margin="0,0,0,8"/>
                        <TextBlock x:Name="SecureDetailsTextBlock" 
                                   FontSize="14" 
                                   Foreground="#065F46" 
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Close Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CloseButton" 
                    Content="Close" 
                    Width="100" 
                    Height="36" 
                    Style="{StaticResource PrimaryButton}" 
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

