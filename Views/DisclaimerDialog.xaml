<Window x:Class="Dorothy.Views.DisclaimerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Advanced Settings – Authorization Required"
        Icon="pack://application:,,,/Resources/icon.ico"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource PrimaryBg}"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Advanced Settings – Authorization Required"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimary}"
                       Margin="0,0,0,4"/>
            <TextBlock Text="Authorization Notice"
                       FontSize="12"
                       Foreground="{DynamicResource TextMuted}"
                       Margin="0,0,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{DynamicResource SecondaryBg}"
                BorderBrush="{DynamicResource BorderDefault}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <Border Background="{DynamicResource FieldWarningBg}"
                            BorderBrush="{DynamicResource FieldWarningBorder}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="16"
                            Margin="0,0,0,24"
                            HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠"
                                       FontSize="20"
                                       Foreground="{DynamicResource WarningAmber}"
                                       Margin="0,0,12,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="Restricted Area – Authorized Operations Only"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource WarningAmber}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <TextBlock FontSize="13"
                               Foreground="{DynamicResource TextSecondary}"
                               TextWrapping="Wrap"
                               LineHeight="24"
                               Margin="0,0,0,20">
                        <Run Text="This section of the application is intended " FontSize="13"/>
                        <Run Text="only for authorized operations" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                        <Run Text=" and is " FontSize="13"/>
                        <Run Text="not to be used for testing or experimentation" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                        <Run Text=". Use of these functions without proper authorization is " FontSize="13"/>
                        <Run Text="strictly prohibited" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                        <Run Text="." FontSize="13"/>
                        <LineBreak/><LineBreak/>
                        <Run Text="Before using this tool, you " FontSize="13"/>
                        <Run Text="must obtain an official written authorization letter" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                        <Run Text=" from all related organizations or stakeholders and submit it to the system administrator. " FontSize="13"/>
                        <Run Text="Do not proceed unless you have received explicit confirmation of approval" FontSize="13" FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                        <Run Text="." FontSize="13"/>
                    </TextBlock>

                    <Border Background="{DynamicResource TertiaryBg}"
                            BorderBrush="{DynamicResource BorderDefault}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="16"
                            Margin="0,8,0,0">
                        <TextBlock FontSize="12"
                                   Foreground="{DynamicResource TextSecondary}"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   LineHeight="20">
                            <Run Text="Unauthorized use " FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                            <Run Text="may be illegal and is " FontWeight="SemiBold"/>
                            <Run Text="strictly prohibited" FontWeight="Bold" Foreground="{DynamicResource ErrorRed}"/>
                            <Run Text="." FontWeight="SemiBold"/>
                        </TextBlock>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="BackButton"
                        Content="Back"
                        Width="120"
                        Height="42"
                        FontSize="14"
                        Margin="0,0,12,0"
                        Click="BackButton_Click"
                        Background="{DynamicResource BorderDefault}"
                        Foreground="{DynamicResource TextPrimary}"
                        BorderBrush="{DynamicResource BorderHover}"
                        BorderThickness="1"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6"
                                                Padding="12,8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource BorderHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource TertiaryBg}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button x:Name="ContinueButton"
                        Content="Acknowledged"
                        Width="180"
                        Height="42"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Click="ContinueButton_Click"
                        Background="{DynamicResource AccentBlue}"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                Padding="12,8">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBlueHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBlueHover}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
